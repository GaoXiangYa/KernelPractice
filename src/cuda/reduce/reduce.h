#include <cstddef>

void reduce_v0(float* input, size_t input_count, float* output);

void reduce_v1(float* input, size_t input_count, float* output);

void reduce_v2(float* input, size_t input_count, float* output);